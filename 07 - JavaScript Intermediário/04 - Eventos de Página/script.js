function carregou(){
    alert('Página carregada');
}

/** jQuerry para alertar quando o usuário for abandonar a página
 * $(document).bind("mouseleave", function(e) {
    if (e.pageY - $(window).scrollTop() <= 1) {    
        alert('Tem certeza que quer fechar a aba? :(');
    }
});
 */